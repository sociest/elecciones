---
import Layout from '../layouts/Layout.astro';
import EncuestasPage from '../components/encuestas/EncuestasPage';
---

<Layout title="Monitor Electoral Bolivia | Encuestas">
  <div
    class="min-h-screen pt-32 pb-24 px-6 relative overflow-hidden bg-slate-50"
  >
    <div
      class="max-w-7xl mx-auto relative z-10 transition-all duration-1000 slide-up pt-12"
    >
      <h1
        class="text-5xl md:text-8xl font-black text-slate-900 underline underline-offset-8 decoration-primary-green decoration-4 tracking-tighter leading-[0.85] text-balance mb-16"
      >
        Encuestas y Sondeos
      </h1>
      <p class="text-xl text-slate-700 max-w-2xl leading-relaxed mb-20">
        Explora las encuestas de intención de voto y otras mediciones de opinión
        pública relacionadas con las Elecciones 2026.
      </p>
      <EncuestasPage client:only="react" />
    </div>
  </div>
</Layout>

<style>
  .slide-up {
    animation: slideUp 1s ease-out forwards;
    opacity: 0;
  }

  @keyframes slideUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>
